import { SchedulingForm } from "../../components/SchedulingForm"

export const SchedulingPage = () => {

    return (
        <section>
            <h2>Agendamento</h2>
            <div>
                <SchedulingForm />
            </div>
        </section>
    )
}